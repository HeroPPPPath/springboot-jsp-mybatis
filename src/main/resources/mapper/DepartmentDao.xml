<?xml version="1.0" encoding="UTF-8"?>
<!-- 映射文件 -->
<!DOCTYPE mapper 
PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.DepartmentDao">

	<select resultType="com.example.demo.entity.Department" id="search">
		select * from dep
	</select>

	<select resultType="com.example.demo.entity.Department" id="searchById">
		select * from dep where id=#{id}
	</select>

	<insert id="add">
		insert into dep(name)values(#{name})
	</insert>

	<update id="update">
		update dep set name=#{name} where id=#{id}
	</update>

	<delete id="delete">
		delete from dep where id=#{id}
	</delete>

</mapper>
    